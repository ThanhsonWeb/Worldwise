# React + Vite Project

This is a advance React project ! I tried my best to build this again by myself ! (of course some helps from AI 😉) but after code it again i UNDERSTAND 90% of it ! how it actually work ...
  
## 🚀 Features
- Fast development with Vite
- React components and hooks
- Context API for state management (Auth, Cities, etc.)
- Tailwind CSS for styling
- JSON Server integration for mock API
- ESLint configuration for clean code

## 📦 Installation
Clone the repository and install dependencies:

```bash
git clone <your-repo-url>
cd <your-project-folder>
npm install
